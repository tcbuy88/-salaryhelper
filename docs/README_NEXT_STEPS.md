# 下一步（联调清单）

1. 本地运行静态 demo：用 nginx 或 file:// 打开 static/index.html
2. 启动 Prism mock 或 server stub：docker compose -f docker-compose-full.yml up --build
3. 联调：登录 -> 创建会话 -> 发送消息 -> 生成文书 -> 上传证据 -> 支付模拟
